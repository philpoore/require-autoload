{
  "name": "require-autoload",
  "version": "1.0.0",
  "description": "Require Autoload",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Phil Poore",
  "license": "ISC",
  "dependencies": {
    "debug": "^2.2.0"
  }
}
